import { buildMeta } from '../lib/seo'

export const metadata = buildMeta({
  title: 'Returns & Exchanges — Vibin Apparel',
  description: 'Easy 30-day returns and exchanges on all Vibin Apparel SS26 orders. Unworn, unaltered items with tags attached. Jacksonville streetwear you can buy with confidence.',
  path: '/returns'
})

export { default } from './ReturnsClient'
